"""Domain-specific EnvironmentAdapter implementations.

Each dispatcher wraps a concrete environment (e.g. purchase approval) and
provides the generic runner with observe/dispatch/advance/snapshot hooks.
"""
